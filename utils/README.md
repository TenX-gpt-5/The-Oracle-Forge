# Utils Directory

This directory is reserved for shared utility code that should be documented independently from the main runtime modules.

For each utility added here, include:

- what it does
- where it is used
- one usage example
- any tests that cover it

Current note:

Most implementation utilities still live under [src/tools](/Users/gersumasfaw/week8_9/src/tools) and related runtime modules. This directory exists to match the submission-oriented repo shape expected by the programme manual.
